{
  "name": "@towerforge/desktop",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:dmg-background": "node scripts/build-dmg-background.mjs",
    "prepare-runtime": "node scripts/prepare-runtime.mjs",
    "tauri": "tauri",
    "dev": "npm run prepare-runtime && tauri dev",
    "build": "node scripts/build.mjs",
    "bundle": "npm run build",
    "verify:macos-bundle": "node scripts/verify-macos-bundle.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
